from django.urls import path
from . import views

urlpatterns = [
    # Puedes comentar o eliminar esta línea si no deseas tener rutas accesibles desde la web.
    # path('reporte-mensual/', views.generate_monthly_report, name='reporte_mensual'),
]
